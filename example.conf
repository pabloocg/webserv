http {

	include mime.types;
    #The first server will be the default server
    server {
        listen       8081;
        server_addr 127.0.0.1;
        server_name  localhost;

        client_max_body_size 100; #In megabytes

        #access_log  logs/host.access.log  main;
        error_page  404              /404.html;

        location dir/ { #Virtual directory
            http_methods GET POST; #Accepted HTTP methods
            root   html; # Path of existing root directory
            index  index.html; # Index file
            autoindex on; #Default off
            upload on; #Default off
            path_upload /uploads;
            #CGI parameters
        }

        location dir/noticias_2/ { #Virtual directory
            http_methods GET POST; #Accepted HTTP methods
            root   html; # Path of existing root directory
            index  index.html; # Index file
            autoindex on; #Default off
            upload on; #Default off
            path_upload /uploads;
            #CGI parameters
            auth_basic "Login with credentials";        #For Basic Auth
            auth_basic_user_file auth/.htpasswd;  #For Basic Auth
        }

        # redirect server error pages to the static page /50x.html
        error_page   500 502 503 504  /50x.html;

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}
    }

    server {
        listen       8080;
        server_addr 127.0.0.1;
        server_name  alias;

        location / {
            root   html;
            index  index.html;
        }
    }
}